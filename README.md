# Data-Frames website

Marketing site for [data-frames.com](https://data-frames.com), a single
self-contained static site in `website/`, deployed to GitHub Pages by the
workflow in `.github/workflows/`.

To publish a change: edit files in `website/`, commit, and push to `main` —
the deploy workflow republishes automatically.
